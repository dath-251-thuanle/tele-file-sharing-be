openapi: 3.0.0
info:
  title: File Sharing API
  description: API cho dịch vụ chia sẻ file.
  version: 1.0.0-rc.1

paths:
  /api/me:
    get:
      summary: Lấy thông tin người dùng hiện tại
      description: |
        Trả về thông tin của người dùng đang thực hiện request.
        Middleware sẽ tự động tạo người dùng mới nếu chưa tồn tại dựa trên header.
      tags:
        - Users
      parameters:
        - $ref: "#/components/parameters/TelegramUserId"
        - $ref: "#/components/parameters/TelegramUsername"
      responses:
        "200":
          description: Thành công. Trả về thông tin người dùng.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          description: Bad Request - Header bị thiếu hoặc không hợp lệ.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal Server Error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /v1/files:
    post:
      summary: Khởi tạo upload, trả URL hoặc nhận file
      description: |
        Cho phép người dùng khởi tạo upload file.  
        Backend sẽ:
        - Xác thực người dùng dựa trên Telegram headers.  
        - Tạo bản ghi `files` trong DB (trạng thái `pending`).  
        - Tạo URL upload (presigned URL) hoặc chấp nhận file trực tiếp.  
        - Trả về `file_id`, `object_key`, và `upload_url` (nếu có).
      tags:
        - Files
      parameters:
        - $ref: "#/components/parameters/TelegramUserId"
        - $ref: "#/components/parameters/TelegramUsername"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - filename
                - size
              properties:
                filename:
                  type: string
                  example: "photo.jpg"
                size:
                  type: integer
                  example: 204800
                mime_type:
                  type: string
                  example: "image/jpeg"
                created_at:
                  type: string
                  formant: date-time
                  example: "2025-11-09T14:00:00Z"
                updated_at:
                  type: string
                  format: date-time
                  example: "2025-11-09T14:00:00Z"
      responses:
        "200":
          description: Thành công – trả về thông tin upload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileInitResponse"
        "400":
          description: Thiếu hoặc sai dữ liệu đầu vào
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Lỗi hệ thống
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      summary: Liệt kê file của người dùng hiện tại
      description: |
        Trả về danh sách file thuộc sở hữu của người dùng đang thực hiện request.
        Người dùng được xác định thông qua Telegram headers.
      tags:
        - Files
      parameters:
        - $ref: "#/components/parameters/TelegramUserId"
        - $ref: "#/components/parameters/TelegramUsername"
      responses:
        "200":
          description: Thành công - Trả về danh sách file.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/File"
        "400":
          description: Bad Request - Thiếu hoặc sai header.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal Server Error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /v1/files/{file_id}/report-complete:
    post:
      summary: Báo cáo hoàn tất upload file
      description: |
        Cho phép người dùng gửi báo cáo về việc hoàn tất upload file.
        Backend sẽ:
        - Xác thực người dùng dựa trên Telegram headers.
        - Kiểm tra quyền sở hữu file.
        - Tạo bản ghi báo cáo (upload_reports).
        - Cập nhật trạng thái file (completed/failed).
        - Trả về thông tin báo cáo vừa tạo.
      tags:
        - Files
      parameters:
        - name: file_id
          in: path
          required: true
          description: ID của file cần báo cáo
          schema:
            type: integer
            format: int64
        - $ref: "#/components/parameters/TelegramUserId"
        - $ref: "#/components/parameters/TelegramUsername"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReportUploadCompleteRequest"
      responses:
        "201":
          description: Thành công báo cáo được tạo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportUploadCompleteResponse"
        "400":
          description: Thiếu hoặc sai dữ liệu đầu vào
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: File không tồn tại hoặc không có quyền truy cập
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Lỗi hệ thống
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /v1/files/{file_id}/report:
    get:
      summary: Lấy báo cáo upload hoàn tất mới nhất
      description: |
        Trả về báo cáo upload hoàn tất mới nhất cho file được chỉ định.
        Người dùng chỉ có thể xem báo cáo của file mình sở hữu.
      tags:
        - Files
      parameters:
        - name: file_id
          in: path
          required: true
          description: ID của file
          schema:
            type: integer
            format: int64
        - $ref: "#/components/parameters/TelegramUserId"
        - $ref: "#/components/parameters/TelegramUsername"
      responses:
        "200":
          description: Thành công – trả về báo cáo upload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadReport"
        "400":
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Báo cáo không tồn tại hoặc không có quyền truy cập
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /v1/upload-reports:
    get:
      summary: Liệt kê tất cả báo cáo upload của người dùng
      description: |
        Trả về danh sách tất cả báo cáo upload của người dùng hiện tại,
        được sắp xếp theo thời gian gần nhất trước.
        Hỗ trợ phân trang.
      tags:
        - Files
      parameters:
        - $ref: "#/components/parameters/TelegramUserId"
        - $ref: "#/components/parameters/TelegramUsername"
        - name: limit
          in: query
          description: Số lượng báo cáo tối đa trả về (mặc định 20, tối đa 100)
          schema:
            type: integer
            default: 20
            maximum: 100
        - name: offset
          in: query
          description: Vị trí bắt đầu (mặc định 0)
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Thành công danh sách báo cáo
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UploadReport"
        "400":
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: User not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /v1/shares:
    post:
      summary: Tạo 1 chia sẻ mới cho file - Test push lên branch tin 
      description: 
        Tạo một link chia sẻ mới cho một file.
        Người dùng phải là chủ sở hữu của file.
        Backend sẽ tự động tạo một 'hash' duy nhất cho link.
      tags: 
        - Shares
      paramethers:
        - $ref: "#/components/parameters/TelegramUserId"
        - $ref: "#/components/parameters/TelegramUsername"
      requestBody:
          required: true
      content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateShareRequest"

      responses:
        "201":
          description: Tạo chia sẻ thành công. Trả về thông tin share.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Share"
        "400":
          description: Bad Request - Thiếu file_id hoặc dữ liệu không hợp lệ.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: File không tồn tại hoặc không có quyền.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description: Internal Server Error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"


  /api/v1/shares/{id}/revoke:
    post:
      summary: Thu hồi link chia sẻ
      description: Đánh dấu một link chia sẻ là đã thu hồi.
      tags:
        - Shares
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
        - $ref: "#/components/parameters/TelegramUserId"
        - $ref: "#/components/parameters/TelegramUsername"
      responses:
        "200":
          description: Thành công
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShareRevokeResponse"  # <--- Đã sửa dòng này
        "400":
          description: Lỗi dữ liệu đầu vào
          content:
             application/json:
               schema:
                 $ref: "#/components/schemas/Error"
        "404":
          description: Không tìm thấy hoặc không có quyền
          content:
             application/json:
               schema:
                 $ref: "#/components/schemas/Error"

components:
  parameters:
    TelegramUserId:
      name: X-Telegram-User-Id
      in: header
      required: true
      description: ID người dùng từ Telegram.
      schema:
        type: integer
        format: int64
    TelegramUsername:
      name: X-Telegram-Username
      in: header
      required: true
      description: Username người dùng từ Telegram.
      schema:
        type: string
  schemas:
    File:
      type: object
      properties:
        id:
          type: integer
          example: 101
        object_key:
          type: string
          example: "uploads/2025/11/09/photo_abc123.jpg"
        filename:
          type: string
          example: "photo.jpg"
        size:
          type: integer
          example: 204800
        mime:
          type: string
          example: "image/jpeg"
        status:
          type: string
          example: "pending"
        created_at:
          type: string
          format: date-time
          example: "2025-11-09T14:00:00Z"
        updated_at:
          type: string
          format: date-time
          example: "2025-11-09T14:00:00Z"

    User:
      type: object
      properties:
        id:
          type: integer
          description: ID nội bộ của user.
        telegram_id:
          type: integer
          format: int64
          description: ID từ Telegram.
        username:
          type: string
          description: Username từ Telegram.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    
    Error:
      type: object
      properties:
        error:
          type: string

    ReportUploadCompleteRequest:
      type: object
      required:
        - status
        - report_type
      properties:
        status:
          type: string
          enum: [completed, failed]
          description: Trạng thái upload (completed hoặc failed)
          example: "completed"
        report_type:
          type: string
          enum: [success, error, warning]
          description: Loại báo cáo
          example: "success"
        message:
          type: string
          description: Thông điệp mô tả
          example: "Upload completed successfully"
        error_code:
          type: string
          description: Mã lỗi (nếu có)
          example: "FILE_TOO_LARGE"
        error_message:
          type: string
          description: Chi tiết lỗi
          example: "File size exceeds limit"
        file_checksum:
          type: string
          description: Checksum của file (MD5/SHA256)
          example: "a1b2c3d4e5f6"
        file_size_actual:
          type: integer
          format: int64
          description: Kích thước thực tế của file (bytes)
          example: 204800
        upload_duration_ms:
          type: integer
          description: Thời gian upload (milliseconds)
          example: 5000
        bandwidth_kbps:
          type: number
          format: double
          description: Tốc độ upload (Kbps)
          example: 512.5

    ReportUploadCompleteResponse:
      type: object
      properties:
        report_id:
          type: integer
          format: int64
          description: ID báo cáo vừa tạo
          example: 1
        file_id:
          type: integer
          format: int64
          description: ID file
          example: 101
        status:
          type: string
          description: Trạng thái báo cáo
          example: "completed"
        report_type:
          type: string
          description: Loại báo cáo
          example: "success"
        message:
          type: string
          description: Thông điệp
          example: "Upload completed successfully"
        reported_at:
          type: string
          format: date-time
          description: Thời gian báo cáo
          example: "2025-11-12T10:30:00Z"

    UploadReport:
      type: object
      properties:
        id:
          type: integer
          format: int64
          description: ID báo cáo
          example: 1
        file_id:
          type: integer
          format: int64
          description: ID file
          example: 101
        owner_user_id:
          type: integer
          format: int64
          description: ID chủ sở hữu file
          example: 1
        status:
          type: string
          enum: [pending, completed, failed, processing]
          description: Trạng thái báo cáo
          example: "completed"
        report_type:
          type: string
          enum: [success, error, warning]
          description: Loại báo cáo
          example: "success"
        message:
          type: string
          description: Thông điệp mô tả
          example: "Upload completed successfully"
        error_code:
          type: string
          description: Mã lỗi
          example: "FILE_TOO_LARGE"
        error_message:
          type: string
          description: Chi tiết lỗi
          example: "File size exceeds limit"
        file_checksum:
          type: string
          description: Checksum của file
          example: "a1b2c3d4e5f6"
        file_size_actual:
          type: integer
          format: int64
          description: Kích thước file (bytes)
          example: 204800
        upload_duration_ms:
          type: integer
          description: Thời gian upload (milliseconds)
          example: 5000
        bandwidth_kbps:
          type: number
          format: double
          description: Tốc độ upload (Kbps)
          example: 512.5
        reported_at:
          type: string
          format: date-time
          description: Thời gian báo cáo
          example: "2025-11-12T10:30:00Z"
        updated_at:
          type: string
          format: date-time
          description: Thời gian cập nhật lần cuối
          example: "2025-11-12T10:30:00Z"
  
    FileInitResponse:
      type: object
      properties:
        file_id:
          type: integer
          example: 101
        object_key:
          type: string
          example: "uploads/2025/11/09/photo_abc123.jpg"
        upload_url:
          type: string
          example: "https://minio.example.com/presigned/abc123"
        status:
          type: string
          example: "pending"
        created_at:
          type: string
          format: date-time
          example: "2025-11-09T14:00:00Z"
        updated_at:
          type: string
          format: date-time
          example: "2025-11-09T14:00:00Z"
    
    Share:
      type: object
      description: Thông tin về liên kết chia sẻ.
      properties:
        id:
          type: integer
          format: int64
          example: 1
        file_id:
          type: integer
          format: int64
          example: 101
        owner_user_id:
          type: integer
          format: int64
          example: 5
        hash:
          type: string
          description: Chuỗi định danh duy nhất của link share.
          example: "a1b2c3d4"
        revoked:
          type: boolean
          description: Trạng thái thu hồi (true là đã thu hồi).
          example: false
        expires_at:
          type: string
          format: date-time
          nullable: true
          description: Thời gian hết hạn link (null là vĩnh viễn).
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CreateShareRequest:
      type: object
      required:
        - file_id
      properties:
        file_id:
          type: integer
          format: int64
          description: ID của file cần chia sẻ.
          example: 101
        expires_at:
          type: string
          format: date-time
          nullable: true
          description: Thời gian hết hạn (null hoặc bỏ qua là vĩnh viễn).
          example: "2025-12-31T23:59:59Z"

    ShareRevokeResponse:
      type: object
      description: Phản hồi khi thu hồi share thành công.
      properties:
        message:
          type: string
          example: "Share revoked successfully"
        share_id:
          type: integer
          format: int64
          example: 1
        status:
          type: string
          example: "revoked"